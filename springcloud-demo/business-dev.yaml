# 管理端口的开放配置
management:
  endpoints:
    web:
      exposure:
        include: env,encrypt
  endpoint:
    env:
      enabled: true
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      # 注意，不能用default-zone version:Greenwich.SR2
      defaultZone: http://localhost:9010/eureka/

spring:
  profiles:
    active: dev
  datasource:
    druid:
      #########################################################
      ## JDBC连接 配置
      url: ''
      username: 'root'
      password: 'pwsd123456'
      # 8.0的Driver
      driver-class-name: 'com.mysql.cj.jdbc.Driver'
      ########### 连接池配置